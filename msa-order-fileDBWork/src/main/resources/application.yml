server:
  port: 8089

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/whatap?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Aa2011287~!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    databse: mysql
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    org.springframework: DEBUG